name: python-ci

on:
  pull_request:
    paths:
      - "services/python/**"
      - ".github/workflows/python-ci.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  python:
    name: lint · test · alembic
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/python
    steps:
      - uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"

      - name: Sync (frozen)
        run: uv sync --frozen

      - name: Lockfile in sync
        run: uv lock --check

      - name: Lint
        run: uv run ruff check .

      - name: Format check
        run: uv run ruff format --check .

      - name: Tests
        run: uv run pytest -m "not gpu" -q

      - name: Alembic single head assert
        run: |
          uv run alembic heads
          test "$(uv run alembic heads | grep -c '(head)')" -le 1 \
            || { echo '::error:: alembic 出现多头，迁移历史必须线性单头（0 头=暂无迁移，允许）'; exit 1; }
          # 注：alembic check（模型 vs 迁移一致）需连库执行；M2 接入 PG 后改为带 DB 的集成步骤再启用
