# 46 · 英文小说阅读功能 · 四官拷问合流报告

> 拷问对象：`local/reading-design-draft.md` v1 → v2 定稿（docs/45）。
> 四官：A 数据模型（V-1…V-20）/ B 业务逻辑（B-1…B-22）/ M 模块工程（M-1…M-18）/
> U UI-UX（U-1…U-22）；本文档 = 合流记录（分歧裁决 + 必改项落地口径），逐条可回溯 docs/45。
> 证据链：VoiceStudio 0.5.1 本机源码复核（AGPL-3.0-only，禁拷贝）+ 本地模型能力清单
> （KittenTTS mini = 78MB ONNX/CPU 实时/8 音色/Apache-2.0；OmniVoice = TTS 非 ASR、
> 600+ 语种但 CC-BY-NC；CosyVoice3/VoxCPM2 需 GPU；GPT-SoVITS 中文优先）。

## 1. 裁决表（四官交叉冲突 → 合流拍板）

| # | 问题 | 分歧 | 合流裁决（docs/45 落点） |
|---|---|---|---|
| C-1 | 设计语言 | MASTER 暖色 vs u-* 实况 vs mobile-soft s-* | **u-*（当前 14 页真实语言）**；新类 u-rd-*/u-bs-* 前缀；正文衬线豁免（U 系列） |
| C-2 | 词卡形态 | 词旁 popover（截图）vs 底部 sheet | **底部 sheet**（480px 容器 + u-sheet 既有语言；U-3） |
| C-3 | 生词本入口 | 独立 /m/vocab vs 复用 /m/learn/words | **/m/vocab 唯一**；学习页「我的单词」摘要+CTA 收敛（U-5） |
| C-4 | 查词未命中 | 404+45003 vs 200+flag | **404/45003** + 前端「暂无释义 + 外链 + 可加生词本」（V-9/B-9） |
| C-5 | reading_tts 桶 | 300/时（M）vs 每请求扣（B 反对） | **600/时 + 只扣真实合成**（命中 0 扣；prepare 按章扣 1；B-4） |
| C-6 | 任务进度通道 | plan+轮询 vs SSE vs DB 表 | **SSE 流（task_start→sentence_progress*→task_done）+ tts_tasks 快照兜底**；after_seq 登记 P2（B-2/V-11） |
| C-7 | 倍速实现 | 服务端 rate 多档 vs 前端 playbackRate | **前端 playbackRate**（免每档重合成；B 系列量化：4 档 × 200 句 = 800 次合成爆桶） |
| C-8 | 联调测试页 | AGENTS 强制 test-only 端点 vs 真路由前例 | **A 落法**（ReadingPreview 直调真路由；无 Fake 黑盒，真数据即联调面；M-9/CommunityPreview 前例） |
| C-9 | 词典 forms | exchange jsonb 仅存 vs 反向索引表 | **存 exchange jsonb + dictionary_forms 反向表**（"inventions"→"invention"；A 官 8 表口径） |
| C-10 | 听书对齐 | 句级 vs 词级（whisper 强制对齐） | **句级 v1**（逐 span 时长累加，免 ASR）；词级登记 P2（VoiceStudio 复核：其词级亦靠 WhisperX 强制对齐，RTF 成本） |

## 2. A 官要点（数据模型 · 意见已全部落地 docs/45 §2）

- **P0×3**：① `book_chapters.content_version` 版本守卫 + annotations/progress 冗余冗余该列
  （文本修订后旧批注**不静默错位**，否则全量重锚；V-6）；② 埋点非「零改动」——events.event_type
  是硬编码 CHECK（V-17 → 迁移 0010 扩 5 值，0006 NOT VALID + VALIDATE 姿势）；
  ③ tts_tasks.error 应 jsonb（对齐 attempts.error 惯例；V-11）。
- **P1×3**：④ dictionary_entries.exchange 用 jsonb（词形；V-5）；⑤ docs/10「24 表」口径滞后
  （实际 30 表）——顺手更正；⑥ user_vocabulary 复合索引 (user_id,status,created_at)+
  chapter_id（回原文）+ first-write-wins（V-10）。
- 否决复合锚 (para, sentence, word, offset_in_word)：对重切分同样脆弱且与 user_vocabulary
  词级职责重叠（V-6）；word 归一化单函数 `reading/normalize.py`（seed 与查询共用，V-8）；
  frequency = frq 优先 bnc 兜底（**位次越小越常见**；V-7）。

## 3. B 官要点（业务逻辑 · docs/45 §4/§5）

- **B-2 纠错**：前端 SSE helper 在 `apps/web/src/audio/sse.ts`；openSseFetch 支持 FormData 体
  （prepare 走 FormData POST 零扩展）；reading SSE 与 practice turn SSE **独立协议**（golden 双端
  分文件：后端 `tests/fixtures/sse_reading_event_cases.json` 未建——以事件模型 + 路由断言覆盖，
  前端 golden 走 parseSseBuffer 单元断言，docs/46 注明与 docs/16 的 practice golden 分离）；
- **B-4**：只扣真实合成；429 为裸 HTTPException（前端按 resp.status===429 + Retry-After 暂停提示）；
- **B-15 四处同步**：EventTypes 常量 ✓ + analytics CHECK ✓ + 迁移 ✓ + 前端 EventName ✓（docs/45 §6）；
  服务端白名单 ALLOWED_TYPES 由 EventTypes 派生自动覆盖 ✓；
- **目标口径**：音频端点免 Envelope（B 例外登记 docs/21 §1.1）；加载音频统一 loadAudioBlob
  （fetch + Bearer，原生 <audio> 不能带头）✓ 已在 useChapterTts 实现。

## 4. M 官要点（工程规范 · docs/45 §8/§9）

- reading/ 三件套（split 纯函数 / service 薄函数组 / orchestrator 后台编排）——不做 400 行巨类；
  routes 拆 reading.py + reading_tts.py（SSE 生成器独立）;
- 契约刷新：本机用 `app.openapi()` 导出 + `pnpm gen:api`（refresh-openapi.ps1 需起服务，
  本批次离线完成；docs/21 ops 21→41 + 限流表 reading_tts 行 + 例外面板 + §3 契约要点，M-13）；
- 前端 fe-08 门禁：阅读器拆分 composables + 弹层组件，**不进灰名单**（M-8）；
- docs 落位：45/46 序号无冲突；docs/10 增补（写方矩阵 + 8 表 + 30→38 表口径）；error-codes 45xxx
  先登记；README 索引 + 演进方向①落地标注；worklog 主线 + 安卓日志（UI 部分），署名
  「组长 LHRCarrier（AI 代工，2026-09-10）」。

## 5. U 官要点（UI/UX · docs/45 §6）

- trim：① TabBar group 增补（U-4）；② 生词本入口去重（U-5）；③ 词级读音端点 U-9 → 已补
  `GET /reading/tts/word/{word}`（docs/45 §4）；④ 类名前缀 u-rd-*/u-bs-*/u-bd-*/u-vb-*（U-22）；
- 交互规格：点词=词卡 sheet（主 CTA 炭黑，截图蓝为强调色）；划词=selectionchange 200ms 防抖 +
  句内锚 + 听书互斥（划选即 pause）；听书=当前句背景高亮 + 窗口化滚动(15%~75%) + 预取 3 +
  Blob LRU ≤12；设置=字号/行距/主题/音色 sheet 即时生效；触点 ≥44px / aria 标注 / safe-area /
  暗黑对比度 ≥13:1 正文（token 表与细则见 docs/45 §6 与 reader-uic.css 注释）。

## 6. 模型能力清单（选型依据 · 全量核对自 HF cache 文本）

| 模型 | 判定 | 本功能落点 |
|---|---|---|
| KittenML/kitten-tts-mini-0.8 | TTS · 英文向 · ONNX 78MB · CPU 实时 · Apache-2.0 · 8 音色 | **本地引擎（kitten provider，可选启用）** |
| FunAudioLLM/Fun-CosyVoice3-0.5B | TTS · 英文一等（test-en WER 2.24）· Apache-2.0 | 需 GPU（9.3GB）→ 登记后置 |
| k2-fsa/OmniVoice | **TTS**（非 ASR）· 600+ 语种 · RTF 0.025 | 权重 CC-BY-NC **禁商用** → 仅文档提及 |
| openbmb/VoxCPM2 | TTS · 2B · 需 CUDA ≥12 | 登记后置 |
| lj1995/GPT-SoVITS | TTS · 中文优先英文弱 | 不采用 |
| 三个 ASR（sherpa/faster-whisper） | 不用于朗读；词级对齐可用 faster-whisper-large-v3-turbo（CT2 · MIT） | 词级 P2 备选 |

## 7. 遗留与合规注记

- **VoiceStudio AGPL-3.0-only**：本功能仅「模式借鉴」（jobs 状态机/进度事件/整章预合成/
  内容寻址缓存/句级对齐），代码全部自研；未引用其任何源文件；
- ECDICT MIT 许可（skywind3000）；公版书 Gutenberg；kittentts = Apache-2.0 model + 库依赖；
- 数据红线：模型权重/原始 ECDICT 全量（770k 行）/原始音频均未入库（子集 CSV 2.4MB 例外已核）。
