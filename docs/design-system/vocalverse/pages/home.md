# Home Page Overrides（今日首页 · /m/home · v3.1）

> **PROJECT:** VocalVerse — 本文件覆盖 MASTER.md 的页面级规则。
> **层级金字塔（UI 即信息）：** 问候（身份）→ 打卡（激励·唯一黄）→ **深蓝渐变焦点卡（今日任务 · 唯一记忆点）**
> → 统计白卡（次级数据）→ 灰轨白 pill 筛选 → 中性列表（轨迹）。每屏 **1 个主 CTA**（开始今日练习）。

## 布局（390px 视口验证）

- 底 `--s-bg #EDF5FE`；字级 **12/14/16/24 四档**（问候 24 / 任务标题 24 / 正文 14 / 标签-注释 12）；
  间距 8/16/24/32；所有文本左对齐至同一基线列
- 头部：问候 h1 24 + 副行 12（4px 下距）· 头像 44 圆（primary-soft 底 + primary-deep 首字母）
- 打卡徽章：黄 pill `#FBBF24` + 深墨字 `#7A4B00`（全屏唯一黄）+ 16px 火苗图标
- **焦点卡**：深蓝渐变 `#1D4ED8→#172554`、圆角 20、padding 20；tint 徽章「今日」（白 18% + 白字）;
  标题白 24；步骤行（白 86% / 完成 = 绿勾 + 白 50% 删除线）；**白实心 CTA**（高 52 胶囊 + 18px 麦图标）;
  右下 120px 声波装饰（白 14%，不抢文字）
- **统计卡**：白卡 + 1px 描边；3 列居中；标签 12 灰 / 数值 **24 粗 tabular**；平均分绿 `#15803D`、
  连续天数**黄星 glyph** `#B45309`（黄色只做星，不做数据底色）
- **分段**：灰轨 `--s-bg-2`（56px、1px 描边）+ **白色浮起 pill** 滑块（150ms translateX）;
  选中项**加粗 + 深墨色**（未选中 14 中灰 500）；图标 16px
- **列表行**：白卡 + 1px 描边；72px 高；**中性图标块**（`--s-bg-2` + `--s-text-2`，统一 22px 图标）;
  标题 14/600、副行 12 灰；数值列 **16/700 文字级蓝 `#0284C7`**（待提升用深墨 `--s-text`）;
  徽章 11px（success 绿底深绿字 / star 黄底棕字 / neutral 灰底）
- 底部留白 140px + safe-area；区块间隔 24，焦点卡与统计之间 24

## 交互

- 分段滑块平移 + 选中加粗（双信号）；行按压 60ms scale(0.98) + 底变 `--s-bg-2`;
  「开始今日练习」→ `/m/chat`（真会话）；会话卡点击跳转；全部可点元素 cursor:pointer + focus-visible
- 空状态：primary-soft 圆角图标底 + 两行文案（不画大插画）

## 数据口径

统计/打卡/最近练习为 M3 演示帧数据（页面内注明）；问候名、CTA 跳转为真实数据链路。
