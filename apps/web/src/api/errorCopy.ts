/**
 * 错误码 → 友好文案映射（docs/api/error-codes.md）。
 * 统一收口，避免把后端英文 detail / 「HTTP 429」直接甩给用户（docs/19 P0-6 / P1-11）。
 * 未登记码回退到 ApiError.message（含 code 前缀保证可排障）。
 */
import { ApiError } from './client'

const COPY: Record<number, string> = {
  40002: '录音太短或为空：请说满约 1 秒后再点 ■ 停止（不消耗题目与额度）',
  40101: '登录状态已过期：请重新登录',
  40301: '没有权限访问该内容',
  40302: '复测未获准：请先完成一次入学测试',
  40303: '请先完成入学测试（本次可跳过），才能开始练习',
  40901: '操作冲突：请稍后重试，勿重复提交',
  40902: '当前状态不允许该操作：请刷新会话后重试',
  40903: '会话已过期：请重试本轮',
  40910: '有进行中的测试：请先完成或放弃上一次，再开始新一轮',
  41001: '音频已过期：请重新录音',
  41301: '音频太大：请缩短后重试',
  42201: '提交的数据有误：请检查后重试',
  42203: '需先完成至少 1 道朗读题，才能提交综合分',
  42901: '今天/本小时的使用额度用完了：请稍后再试',
  42902: '复测冷却中：请稍后再试',
  50001: '服务器处理超时：请重试',
}

export function errorCopy(e: unknown): string {
  if (e instanceof ApiError) {
    return COPY[e.code] ?? `${e.message}（code ${e.code}）`
  }
  return e instanceof Error ? e.message : '发生错误，请重试'
}
