# 管理端控制台 · 环境变量示例（docs/50 §13.1）
#
# 仓库约定：`.env` 不入库，只提交本文件作占位符（同 apps/web）。
# **默认值已写在代码里**（`src/api/client.ts` 按 DEV/PROD 分别给 /manage 与 /console/manage），
# 所以新克隆的仓库不配任何 env 也能正确构建。本文件只用于**覆盖**。
#
# 复制成 .env.development / .env.production 后按需修改。

# ---- 本地开发（pnpm dev，端口 5174）----
# vite.config.ts 已配同语义代理，浏览器看到的是同源，无 CORS
VITE_CONSOLE_BASE=/manage
VITE_OPS_BASE=/api/v1

# ---- 生产构建 ----
# 由 apps/admin/nginx.conf 承担分流（自带 server 块，apps/web/nginx.conf 零改动）：
#   /console/manage/**  → java-api:8080（剥 /console/manage）
#   /console/api/v1/**  → python-api:8000（剥 /console）
# 生产入口：http://<host>:8089/console/
# VITE_CONSOLE_BASE=/console/manage
# VITE_OPS_BASE=/console/api/v1
